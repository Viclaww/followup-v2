import React from 'react';
import { Button } from '@/components/ui/button';

/**
 * Customer
 * Create a floating customer care button that:
 */
export function Customer() {
  return (
    <div className="fixed bottom-4 right-4 z-50">
      <Button>
        Customer
      </Button>
    </div>
  );
}
